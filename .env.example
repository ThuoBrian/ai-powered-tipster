# Copy to .env and fill in when the phase needs it. .env is gitignored —
# never commit real keys.

# Phase 2 — live odds via The Odds API (https://the-odds-api.com/)
THE_ODDS_API_KEY=

# Phase 2 — fixtures and lineups via football-data.org v4 (free tier)
FOOTBALL_DATA_API_KEY=
